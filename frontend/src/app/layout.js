export default function Layout({ children }) {
    console.log("sdfsdf: ",children)
    return (
        <html>
            <body>
                {children}
            </body>
        </html>
    )
}